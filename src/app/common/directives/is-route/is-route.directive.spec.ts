import { IsRouteDirective } from '@directives/is-route/is-route.directive';

describe('IsRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new IsRouteDirective();
    expect(directive).toBeTruthy();
  });
});
