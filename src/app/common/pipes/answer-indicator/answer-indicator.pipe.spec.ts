import { AnswerIndicatorPipe } from './answer-indicator.pipe';

describe('AnswerIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
